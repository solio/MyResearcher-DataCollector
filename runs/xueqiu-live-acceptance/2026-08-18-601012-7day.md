# Xueqiu 7-Day Live Backfill — 601012

## Result

```text
FINAL_STATUS = XUEQIU_7DAY_BACKFILL_PASS
```

本次执行使用用户指定的 production baseline；运行结束后只新增本验收报告，未修改 Collector、browser runtime、parser、backfill 或 persistence 代码。

```text
execution HEAD: d7282da4496f4c6adb6713cdbcab60bb0c0d54af
run_id: 3f238b0b0cf64790972189e4565b6216
source: xueqiu
stock: 601012
acquisition_mode: dedicated-chrome-cdp
browser: official Google Chrome, collector-owned dedicated persistent profile
managed-chromium: NO
playwright.launch: NO
playwright.launch_persistent_context: NO
Apple Events browser control: NO
concurrency: 1
interval: 3–10 seconds
```

执行命令：

```bash
PYTHONPATH=src python -m myresearcher_collector.cli.main backfill \
  --source xueqiu \
  --stock 601012 \
  --days 7 \
  --data-dir runtime/xueqiu-7day-smoke \
  --acquisition-mode dedicated-chrome-cdp \
  --min-interval 3 \
  --max-interval 10 \
  --confirm-live
```

## Runtime result

```text
status: SUCCESS
stop_reason: backfill_range_complete
range_complete: true
from_time: 2026-08-11T16:00:00Z
to_time: 2026-08-18T15:59:59.999999Z
pages_scanned: 20
records_received: 194
records_in_range: 175
records_new: 175
records_existing: 0
records_failed: 0
records_versioned: 0
modified_posts_resolved: 12
earliest_observed_at: 2026-08-11T06:51:54Z
latest_observed_at: 2026-08-18T03:08:29.574130Z
```

`records_in_range=175` 是本次范围内写入的帖子数；`records_received=194` 包含为判断边界而扫描到的范围外列表记录。最终 coverage 为：

```text
covered_from: 2026-08-11T16:00:00Z
covered_to:   2026-08-18T03:32:29.574130Z
```

## SQLite checks

数据目录：

```text
runtime/xueqiu-7day-smoke/collector.db
```

只读 SQLite 检查结果：

```text
source: xueqiu
stock_code: 601012
total_rows: 175
unique_source_item_ids: 175
duplicate_source_item_ids: 0
source_item_id NULL/empty: 0
content_null_count: 0
published_at_null_count: 0
earliest_published_at: 2026-08-11T17:10:00.000000Z
latest_published_at: 2026-08-18T03:08:29.574130Z
```

20 个 `backfill_page_anchors` 已持久化，页号连续为 `1..20`。运行中途检查时已经看到：

```text
中途检查 1: posts=19, anchors=2, coverage=0
中途检查 2: posts=69, anchors=7, coverage=0
中途检查 3: posts=135, anchors=14, coverage=0
```

因此前序页面在命令尚未结束时已经写入 `collector.db`，不是只依赖最终成功状态来证明 page-level durability。

## Modified posts and detail handling

列表中需要 detail lookup 的修改帖子共 12 条，`modified_posts_resolved=12`，`records_failed=0`。持久化结果中这些帖子使用 detail 得到的真实 `created_at` 作为 `published_at`，没有使用列表上的编辑显示时间。

本次运行涉及临时 detail target；production runtime 在完成后报告：

```text
process_cleanup: pid terminated, returncode=0
cdp_port_closed_after_cleanup: true
diagnostics: []
```

## Challenge / focus observations

运行期间出现过带 `<redacted>` 参数的短暂 `?md5__1038=...` 导航，但随后恢复为正常帖子 DOM，未形成持续 challenge loop，也没有被错误判为失败。

Dedicated Chrome runtime 观测结果：

```text
fixed CDP endpoint: http://127.0.0.1:9227
owned_chrome_frontmost_count: 0
final_user_chrome_matches_baseline: true
focus result: PASS
```

用户 Chrome 没有被抢焦点；运行结束后 collector-owned Chrome 进程及 CDP 端口已清理。

## Boundary conclusion

运行扫描到 page 20 后，`records_received` 中包含了早于请求边界的记录；范围内最早持久化帖子为 `2026-08-11T17:10:00Z`，覆盖边界为 `2026-08-11T16:00:00Z`。CLI 以 `backfill_range_complete` 正常结束，而不是因为页数上限、空页、页面尺寸小于 10、selector error 或 pagination failure。

本次未发生真实 correctness bug，因此没有进行代码修复或从 checkpoint 重跑。

## Validation

```text
compileall: PASS
critical Xueqiu/backfill tests: 30 passed
full pytest: 351 passed, 1 xfailed
```

本次只执行了一个 7 天、单股票 backfill；没有自动继续更长范围或多股票任务。
